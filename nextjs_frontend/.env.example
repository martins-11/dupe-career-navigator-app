# Frontend -> backend routing
# The deployment/orchestrator should set these in .env (do not commit real values).
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=

# Optional: used by backend O*NET integration; included here for visibility in local dev.
REACT_APP_ONET_API_KEY=
