import SkillValidationClient from './skillValidationClient';

// PUBLIC_INTERFACE
export default function SkillValidationPage() {
  /** Entry route for the Skill Validation guided chat experience. */
  return <SkillValidationClient />;
}
