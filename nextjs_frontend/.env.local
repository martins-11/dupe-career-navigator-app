# Local dev defaults (NON-SECRET)
# This file is intentionally safe to commit in this dupe repo context; adjust if your workflow
# requires keeping .env.local untracked.

NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
BACKEND_URL=http://localhost:3001
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_LOG_LEVEL=info
